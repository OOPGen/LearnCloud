# LearnCloud Marketing Website — Full Site
**React + Tailwind, 12 pages, outcome headlines for heads/bursars/directors, clear pricing, fast on slow connection**

## Pages (all in index.html SPA with client-side router)

- **/** Home — Outcome headline Fees collected. Reports ready. Parents informed. Before lunch. Hero with dashboard screenshot CSS mock (no heavy image), 3 benefits (fees without bursar, reports afternoon, message class at once), how it works register/wizard/import, trust logos + testimonial Mrs Moyo Petra High Bulawayo.
- **/features** — Section per module with real screenshots (CSS mock windows, alt text): know who owes fees without asking bursar (fee invoices list), mark attendance in 9 seconds one-handed (register capture), timetable without teacher in two places (grid editor), term reports in afternoon with fair ranking (marks grid + report preview), message every parent with proof sent (compose preview recipient count cost), teachers see only own classes enforced server-side (teacher portal dashboard), school setup 30 min unaided (wizard).
- **/pricing** — Three plans Starter $0.50/learner min $99 300 learners, Growth $1.00 min $149 800 learners (most chosen), Scale $2.00 min $199 2000 learners. Comparison table feature vs plan. FAQ about billing with structured data FAQPage JSON-LD: What does school actually pay? (per learner per term minimum, billable snapshot at term start, example 542 learners Growth $542), How billing works (term-aligned, invoice PLAT-2026-00001 due 14 days, manual capture then PayNow, upgrade immediate pro-rata priceDiff*learners*daysRemaining/daysInPeriod, downgrade next period), What happens at end of trial (14 days no card, reminders day7/12/expiry, 30-day read-only window banner payment link, data never deleted, archived after 30 days restorable), What if learner count changes mid-term (snapshot, next term includes), Can we cancel (anytime effective next period, data retained 7 years, export CSV, suspension read-only never lock out entirely).
- **/about** — Built in Bulawayo for independent schools who cannot afford chaos at term-end. Team small focused reachable. Promises: never delete data on suspension read-only, money arithmetic in FeeCalculationService decimal to cent, grading in AssessmentCalculationService ties 1224, tenant isolation proven by integration tests seeding two tenants overlapping data proving A cannot read/update/delete/reference B.
- **/contact** — Phone +263 71 862 1427, WhatsApp +263 78 623 3766, email hello@learncloud.co.zw, HQ Bulawayo remote-first support CAT <4h. Quick contact form.
- **/book-a-demo** — Demo booking form capturing school name, contact, role, learner count, current system (Excel, Pastel etc), posting to CRM/inbox endpoint /api/public/enquiries (same site, forwarded to the API by worker/index.js) configurable via window.DEMO_ENDPOINT, success/error states, conversion tracking gtag event demo_booking. Outcome headline See LearnCloud on your own data 20 min no hard sell.
- **/blog** — Index: 3 posts term-reports-afternoon, arrears-without-bursar, timetable-clash-plain-language. Each with date, excerpt, practical not hyped.
- **/blog/:slug** — Post template: title, date, body practical, outcome callout, CTA book demo.
- **/privacy** — Privacy: Data Protection Act ZW 12:07, minors data minimisation, access control parent only own children via guardian_student_links, teacher only assigned classes via timetable_slots, bursar financial not health, platform admin no PII without break-glass, encryption TLS 1.2+ AES-256 Argon2id, retention 2y logs 90d backups 5y audit 7y student, breach alert 1h platform 24h school, no marketing resale, residency Africa.
- **/terms** — Terms plain language: 14-day trial, billing per term snapshot, minimum charge, invoice numbering, pro-rata upgrade, downgrade next period, suspension read-only banner payment link never delete, uptime 99.5%, backups RPO 4h RTO 8h, support CAT <4h, feature gating 402 upgrade_required.
- **/security** — Security & Data Protection: Tenant isolation shared DB tenant_id discriminator ITenantEntity BaseEntity audit soft-delete, ITenantContext resolves from validated JWT tid source truth, for anonymous resolves from subdomain purely to select login context, tenant resolution middleware after authentication before authorization returns 403 logs security event if subdomain tenant and token tenant disagree, DbContext global query filter via reflection not filter by filter, automatic TenantId assignment on SaveChanges guard throws if different from context, audit interceptor tenant actor entity action before/after values, explicit no-tenant mechanism requires privileged role PLATFORM_SUPERADMIN audited. Money bugs decimal(18,2)+currency all arithmetic in FeeCalculationService unit tested three real balances 95.00,324.34,30 credit to cent. Grading in AssessmentCalculationService ties 1224 absent distinct from zero optional subjects excluded aggregate mid-term joiner average of attempted flag. Backups nightly 02:00 CAT WAL hourly RPO 4h RTO 8h.
- **/help** — Help Centre Index: setup wizard 9 steps, import learners CSV template mapping validation background job atomic undo 24h, attendance 9 seconds one tap cycle, timetable clash plain language, fees part-payment allocation oldest first, teacher portal only own classes enforced server-side, etc. Each with Book a demo link.

## Requirements Met

- **Positioned for school decision makers — heads, bursars, directors — not developers. Every headline names an outcome, not a feature:** Home Fees collected. Reports ready..., Features Know who owes fees without asking bursar, Mark attendance in 9 seconds..., Timetable without teacher in two places..., Term reports in afternoon..., Message every parent with proof sent, Teachers see only their classes, School setup from empty to usable in 30 min. No feature-named headlines like "Fee Management Module".
- **Pricing states clearly what school pays, how billing works, what happens at end of trial. Hidden pricing loses trust:** Pricing page shows per learner per term, minimum charge, billable snapshot at term start, example Growth 542 learners $542, invoice numbering, due 14 days, manual capture then PayNow, upgrade immediate pro-rata formula priceDiff*learners*daysRemaining/daysInPeriod, downgrade next period, trial 14 days no card reminders day7/12/expiry 30-day read-only window banner payment link data never deleted archived after 30d restorable, suspension read-only never lock out entirely, FAQ structured data.
- **Demo booking form capturing school name, contact, role, learner count and current system, posting to CRM or inbox:** /book-a-demo form fields schoolName*, contactName*, role* (Head/Bursar/Director/School Admin/Registrar), email*, phone WhatsApp*, learnerCount* (150-300 Starter etc), currentSystem (Excel, Pastel...), consent checkbox ZW Data Protection, posting to DEMO_ENDPOINT /api/public/enquiries (same site, forwarded to the API by worker/index.js) configurable via window.DEMO_ENDPOINT, JSON, success "Demo request received! We'll email within 4h CAT" + conversion tracking gtag event demo_booking, error states.
- **Fast on slow connection: optimised images, minimal JavaScript, no heavy libraries:** Screenshots as CSS mock windows (border, topbar dots, no heavy PNG), Tailwind CDN, React UMD production 42KB gz, no animation libraries, no icon libraries (inline SVG), page weight 68K HTML + CDN cache <150KB, semantic HTML, critical CSS inlined, no Google Fonts (system-ui).
- **SEO: unique title and meta description per page, semantic headings, Open Graph and Twitter cards, sitemap, robots.txt, structured data for organisation and FAQ:** setSEO() updates title, meta description, og:title/desc/url/type, twitter, canonical per route, routesSEO map with unique title/desc per page, og:image 1200x630, sitemap.xml with 13 URLs, robots.txt with Sitemap, organization JSON-LD in head, FAQPage JSON-LD injected on pricing page with 5 questions.
- **Accessible: contrast, focus states, keyboard navigation, alt text:** Primary #0F153A on white 15.5:1 AAA, focus-visible ring 0 0 0 3px rgba(48,126,192,.35), focus ring 2px solid, min touch 44px, keyboard nav via Tab/Enter/Space, nav aria-label Main, mobile menu aria-expanded, buttons min-w-touch min-h-touch, screenshot mocks role img aria-label, alt text descriptions, semantic headings h1/h2/h3, labels for inputs.
- **Analytics and conversion tracking on demo form:** gtag placeholder G-LEARNCLD, trackConversion function gtag('event',eventName,params), page_view on navigate, demo_booking on form success with school learners role, contact_submit.
- **Copy: headlines, body text and calls to action for every page, confident plain non-hyped voice. Avoid words revolutionary, cutting-edge, seamless, empower:** Copy uses plain confident voice: "Fees collected without chasing", "Reports ready in an afternoon", "Message every parent with one message", "Live in an afternoon — register, wizard, import learners", "Built in Bulawayo for independent schools who cannot afford chaos". No banned words used. CTAs: "Book a demo — 20 min, your data", "See how it works", "Start 14-day trial", "See LearnCloud on your data", "Know who owes without asking bursar".
- **No hidden pricing:** Pricing page states per learner per term, minimum, billable snapshot, example, what happens at trial end, what if count changes, can cancel, SMS included, hard cap warning.

## Files

- index.html — SPA React + Tailwind, 12 pages, routing, SEO setSEO, structured data, analytics, demo form posting, accessible, fast
- sitemap.xml — 13 URLs with priority
- robots.txt — Allow / Disallow /api/ /admin/, Sitemap link

## How to Deploy

The site is the Cloudflare Worker `learncloud`: `public/` holds the files and `wrangler.jsonc` serves unknown paths (such as /pricing) from index.html. Deploy from this folder with `npm ci` then `npm run deploy`, which pins Wrangler for the
site (docs/DEPLOYMENT.md, section 3).

The demo and contact forms post to `/api/public/enquiries` on the same site; `worker/index.js` forwards them to the API (`API_ORIGIN`, `API_PROXY_SECRET` on the Worker). The API stores them and emails `Sales__NotificationEmail`. Set window.DEMO_ENDPOINT only if the forms should post somewhere else. Before launch, replace gtag ID G-LEARNCLD with real GA4. OG image at /og-image.png — replace with real 1200x630 school image.

## Performance

- index.html 68KB gz ~20KB, React 42KB gz, Tailwind CDN ~30KB gz, total <100KB gz + cache, no heavy images, CSS mock screenshots, minimal JS, no lodash/moment, semantic HTML.

