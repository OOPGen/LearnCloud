{
  "name": "learncloud-marketing",
  "private": true,
  "version": "1.0.0",
  "description": "LearnCloud marketing site: static files in public/, plus worker/index.js which forwards the demo and contact forms to the API.",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
